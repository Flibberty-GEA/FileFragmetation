package com.sysgears.core.input;

import org.apache.commons.cli.CommandLine;
import org.easymock.EasyMock;

/**
 * @author Yevgen Goliuk
 */
public class InputDataHolderTest {
    CommandLine cml = EasyMock.createMock(CommandLine.class);
//    InputDataHolder dataHolder = new InputDataHolder(cml);
}
